const fs = require('fs');
const path = require('path');

const dosomething = (done) => {

  //console.log('beforeEach Called');//Check working
  done();
};

  //delete something or do something, can be used to seed some demo data


module.exports = {dosomething};
